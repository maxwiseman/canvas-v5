## v0.4.0

- Add a portable MCP App for interactive upcoming assignments with pagination, links, host theming, and verified dark-mode support.
- Add OAuth discovery, dynamic client registration, PKCE authorization, consent, and scoped JWT access for hosted ChatGPT, Claude, and other remote MCP clients while preserving personal Canvas V5 bearer tokens.
- Fix the ChatGPT assignment widget remaining on its loading state by rendering cached assignments promptly and supporting ChatGPT's documented tool-output compatibility bridge.
- Cap the upcoming-assignment widget at 600 pixels, respect smaller host height limits, and keep long assignment lists scrollable inside the widget.
- Fix ChatGPT OAuth authorization opening a raw JSON redirect response instead of the Canvas V5 sign-in screen.
- Fix the Firefox extension replacing Canvas with a blank page when TanStack Router parses the current URL.
- Show each course's configured or inferred course icon in Command-K search results instead of a generic school icon.
- Render Command-K result icons with the muted foreground color so titles remain the primary visual emphasis.
- Tighten the sidebar search trigger's right padding so its rounded shortcut badge sits concentrically inside the rounded button.
